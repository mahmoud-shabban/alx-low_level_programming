#include "main.h"
#include <ctype.h>

/**
 * _islower - tes docs
 * @c: int
 * Return: inte
 */
/* more headers goes there */

int _islower(int c)
{
	if (islower(c))
	return (1);
	else
	return (0);
}
