#include "main.h"
#include <ctype.h>

/**
 * _isalpha - tes docs
 * @c: int
 * Return: inte
 */
/* more headers goes there */

int _isalpha(int c)
{
	if (isalpha(c))
	return (1);
	else
	return (0);
}
