#include <stdio.h>

/**
 * main - tes docs
 * Return: inte
 */
/* more headers goes there */

int main(void)
{
	char ch;

	for (ch = '0'; ch <= '9'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
