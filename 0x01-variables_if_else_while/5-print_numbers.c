#include <stdio.h>

/**
 * main - tes docs
 * Return: inte
 */
/* more headers goes there */

int main(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
	{
		printf("%d", ch);
	}
	putchar('\n');
	return (0);
}
